name: Testrun

on: [push]

jobs:

  test:

    runs-on: ubuntu-latest

    steps:

      - name: PHP testing actions

        uses: aohycent/bowinc-php@v0.1.0

        with:

          args: php -v && php -m
